'use strict';

module.exports = function(app) {
  //var httpProxy = require('http-proxy');
  //httpProxy.createProxyServer({}).listen(8484);
  //httpProxy.createProxyServer({target: 'http://localhost:3000'}).listen(8484);
};
