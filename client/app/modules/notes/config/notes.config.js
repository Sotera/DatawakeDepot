'use strict';
var app = angular.module('com.module.notes');

app.run(function($rootScope, Note, gettextCatalog) {
  //$rootScope.addMenu(gettextCatalog.getString('Notes'), 'app.notes.list', 'fa-file-o');
  //
  //
  //Note.find(function(data) {
  //  $rootScope.addDashboardBox(gettextCatalog.getString('Notes'), 'bg-blue11', 'ion-clipboard', data.length, 'app.notes.list');
  //});

});
